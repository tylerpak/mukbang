spring:
  application:
    name: backend

  datasource:
    url: jdbc:postgresql://localhost:5424/mukbang_db
    username: yin
    password: yang

  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none

  docker:
    compose:
      enabled: false

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true



server:
  address: 0.0.0.0
  port: 8080
